## Cursor Cloud specific instructions

This repository (`F1_data_ana`) is currently an empty/scaffolding-phase project with only a `README.md`. There is no source code, dependency files, build system, or runnable application as of the initial commit.

### Key facts
- **Repo name**: F1_data_ana (likely a Formula 1 data analysis project)
- **Tech stack**: Not yet established — no `package.json`, `requirements.txt`, `pyproject.toml`, or similar files exist
- **Services**: None to run
- **Tests**: None to run
- **Lint**: No linter configured

### For future agents
- When code and dependencies are added, update the `SetupVmEnvironment` update script accordingly.
- Check for newly added dependency files (`requirements.txt`, `pyproject.toml`, `package.json`, etc.) before attempting installs.
